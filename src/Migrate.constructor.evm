// address immutable Migrate.owner = msg.sender
PUSH1 { prefix: assemble src/Migrate.evm[:1] } 160
MSTORE(0, OR(CALLER, SHL))
CODECOPY(MSIZE, migrate, #migrate)
RETURN(11, 106)

{
    migrate: assemble src/Migrate.evm[21:]
}
