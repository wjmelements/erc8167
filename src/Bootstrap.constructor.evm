// address immutable Bootstrap.owner = msg.sender
PUSH3 { prefix: assemble src/Bootstrap.evm[:3] } 160
MSTORE(0, OR(CALLER, SHL))
CODECOPY(MSIZE, bootstrap, #bootstrap)
RETURN(9, 125)

{
    bootstrap: assemble src/Bootstrap.evm[23:]
}
